#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    while(getline(cin,s)){//idea to read input of string without knowing no of queries
        if(s.empty()){
            break;
        }else{
            cout<<"NO"<<endl;
        }
    }
    
    return 0;
}