#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;

//basic idea is to put drop the yo yo in any opposie corner
int main()
{
    ll t;
    cin >> t;
    while (t--)
    {
        ll n, m, x, y;
        cin >> n >> m >> x >> y;

        cout << 1 << " " << m << " " << n << " " << 1 << endl;
    }
    return 0;
}